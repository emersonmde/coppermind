[application]
# Asset directory: public/assets/ contains CSS, icons, etc.
# Source assets are in assets/, copied to public/assets/ for builds
asset_dir = "public"

[bundle]
identifier = "dev.errorsignal"
publisher = "Matthew Emerson"

[web.watcher]
# When watcher is triggered, regenerate the `index.html`
reload_html = true
# Which files or dirs will be monitored
watch_path = ["src", "public"]

[web.app]
base_path = "/coppermind"
# HTML title tag content
title = "Coppermind"

[web.resource]
# Additional CSS style files
style = []
# Additional JavaScript files
script = []
#script = ["public/coi-serviceworker.min.js"]

[web.resource.dev]
# Javascript code file
# serve: [dev-server] only
script = []
